#!/bin/bash
# Template for gofr-doc script environment.
# Safe to commit (no secrets).

# Default environment mode. Allowed: TEST, PROD
export GOFR_DOC_ENV="${GOFR_DOC_ENV:-TEST}"

# Optional overrides (uncomment as needed):
# export GOFR_DOC_HOST="0.0.0.0"
# export GOFR_DOC_NETWORK="gofr-test-net"

# Load canonical config logic.
# shellcheck source=/dev/null
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/project.env"
